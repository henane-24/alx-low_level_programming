#include <stdio.h>
/**
 * main - prints alpahabet in lowercase
 *
 * Return: 0
 */
int main(void)
{
char c;
c = 'a';
while (c <= 'z')
{if (ch != 'e' && c != 'q')
{
putchar(ch);
}
ch++;
}
putchar('\n');
return (0);
}

