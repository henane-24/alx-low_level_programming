#include <stdio.h>
/**
 * main - prints alpahabet in lowercase
 *
 * Return: 0
 */
int main(void)
{
        int ch;
	ch = 'a'

	while (ch <= 'z')
	if ch == 'e' || 'q'
	putchar(ch);
	putchar('\n');
	return (0);
}

