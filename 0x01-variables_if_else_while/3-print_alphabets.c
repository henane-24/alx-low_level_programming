#include <stdio.h>
#include <ctype.h>
/**
 * main - prints alpahabet in lowercase
 *
 * Return: 0
 */
int main(void)
{
	int lower;
	int upper;

	lower = getchar();
	upper = toupper(lower);
	putchar(lowwer)
	putchar(upper);
	putchar('\n');
	return (0);
}

