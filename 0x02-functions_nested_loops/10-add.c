#include "main.h"
#include <stdio.h>

/**
 * add - check the code
 * @a: the number to be add
 * @b: the number to be add
 * Return: Always 0.
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
